name: fonemas_app
description: "Um app para crianças com dificuldades de fala"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.2.3
  google_fonts: ^8.1.0
  provider: ^6.1.2
  equatable: ^2.0.8
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1
  cloud_firestore: ^6.4.0
  firebase_storage: ^13.4.0
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
